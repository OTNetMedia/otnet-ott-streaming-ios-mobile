import Foundation

/// Copy this file to `Secrets.swift` and replace the placeholder with your
/// OTNet publisher API key. The real `Secrets.swift` is git-ignored so the
/// key never leaves your machine.
///
/// Get a key at https://otnet.io.
enum Secrets {
    static let otNetApiKey = "otn_replace_with_your_publisher_api_key"
}
